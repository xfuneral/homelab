apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: family-status
  namespace: my-apps
spec:
  ingressClassName: traefik
  rules:
    - host: familia.porcocan.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: family-status
                port:
                  number: 80
