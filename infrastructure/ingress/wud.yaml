apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wud
  namespace: apps
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  ingressClassName: traefik
  rules:
    - host: wud.itworksonmycluster.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: wud
                port:
                  number: 80
